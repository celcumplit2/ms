---
import DetailPage from "../../components/DetailPage.astro";
import SiteLayout from "../../layouts/SiteLayout.astro";
import { industries } from "../../data";

export function getStaticPaths() {
  return industries.map((entry) => ({
    params: { slug: entry.slug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const relatedItems = industries.filter((item) => item.slug !== entry.slug).slice(0, 2);
---

<SiteLayout title={`${entry.title} | Moldstud`}>
  <DetailPage entry={entry} relatedTitle="Related industries" relatedItems={relatedItems} relatedHref="/industries" />
</SiteLayout>
